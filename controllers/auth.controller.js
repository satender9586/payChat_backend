const {userSchema} = require("../models/auth.model.js")

// ------------->> user register 

 const register = async (req , res)=>{
    try {
        
    } catch (error) {
        
    }
}

module.exports = {register}